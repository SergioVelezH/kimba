import React from 'react';
import './interfacesContainer.css';

const InterfacesContainer = ({ children }) => {
  return <div className="container">{children}</div>;
};

export default InterfacesContainer;
